# `Triggers` a child pipeline that builds `irdest-android-client`, the irdest-droid
# The script that runs the pipeline is from path provided to `include`

irdest-android-app:
  stage: build
  trigger:
    include: 
    - 'android-components-pipelines/scripts/irdest-android-app-ci.yml'
    strategy: depend